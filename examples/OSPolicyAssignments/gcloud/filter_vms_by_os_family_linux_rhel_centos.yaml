osPolicies:
  - id: remove-pkg-policy
    mode: ENFORCEMENT
    resourceGroups:
      - resources:
          - id: remove-pkg
            pkg:
              desiredState: REMOVED
              yum:
                name: bad-package
instanceFilter:
  osShortNames:
    - rhel
    - centos
rollout:
  disruptionBudget:
    fixed: 10
  minWaitDuration: 300s
